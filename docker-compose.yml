version: '3.8'

services:
  # Your Writespace API
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development
      - PORT=5000
      - DATABASE_URL=postgresql://postgres:adXnWi33M7$@localhost:5432/writespace
      - REDIS_URL=redis://redis_cache:6379
    depends_on:
      - postgres_db
      - redis_cache

  # Your Database
  postgres_db:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=writespace
    volumes:
      - pgdata:/var/lib/postgresql/data

  # Your Redis Cache
  redis_cache:
    image: redis:alpine
    ports:
      - "6379:6379"

volumes:
  pgdata: # Keeps database data safe even if the container stops